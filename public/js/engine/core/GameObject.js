export default class GameObject{

    // create a component list
    // every component had a specific behavior
    // Ex: Position, Sprite, etc (Unity like)
    
    constructor(){
        this.components = []
    }

    init(game){

    }

    addComponent(gameComponent){

    }

    processComponents(){
        // exec update from components
        // exec draw from components
    }

}
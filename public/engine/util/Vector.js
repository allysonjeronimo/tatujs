export default class Vector{
    /**
     * @param {Object} settings 
     * @param {Number} settings.x
     * @param {Number} settings.y
     */
    constructor(settings){
        this.x = settings.x || 0
        this.y = settings.y || 0
    }

    // other functions/methods to manipulate vectors
}